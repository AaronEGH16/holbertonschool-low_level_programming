#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of program executable
 *
 * @argv: array of arguments
 *
 * Return: always return 0
 */

int main(char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
