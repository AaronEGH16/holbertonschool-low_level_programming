#include <stdio.h>

/**
 *main - entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	char text2[53];
	char text1[3];
	text1 = '"';
	text2 = "Programming is like building a multilingual puzzle";
	puts(text1);
	puts(text2);
	return (0);
}
